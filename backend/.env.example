# Optional override for the demo default PIN (2468).
BATWA_ADMIN_PIN=2468

# Optional. A random process-local secret is used when omitted.
BATWA_ADMIN_TOKEN_SECRET=replace-with-a-long-random-secret
BATWA_ADMIN_TOKEN_TTL_SECONDS=3600
